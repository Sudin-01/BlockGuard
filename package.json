{
  "name": "blockchain-agent-permission-system",
  "version": "1.0.0",
  "description": "Blockchain-Based Permission Enforcement for Autonomous AI Agents",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "deploy:local": "hardhat run scripts/deploy.js --network ganache",
    "deploy:test": "hardhat run scripts/deploy.js --network hardhat",
    "node": "hardhat node",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "ethers": "^6.11.0",
    "ganache": "^7.9.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
